@page
@model TasteRestaurant.Pages.FoodTypes.IndexModel
@{
    ViewData["Title"] = "Index";
}

<br />
<h2>Food Types</h2>
<br />
<p>
    <a asp-page="Create" class="btn btn-primary">
        <span class="glyphicon glyphicon-plus"></span>
        Create New
    </a>
</p>

<div class="form-border">
    <table class="table">
        <tr>
            <th>@Html.DisplayNameFor(m => m.DefaultFoodType.Name)</th>
            <th></th>
        </tr>
        @foreach (var item in Model.FoodTypes)
        {
            <tr>
                <td>@Html.DisplayFor(m => item.Name)</td>
                <td>
                    <partial name="_TableButtonsPartial" model="new SingleButtonPartial {  Id = item.Id }" />
                </td>
            </tr>
        }
    </table>
</div>

